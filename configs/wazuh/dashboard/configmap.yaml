# configs/wazuh/dashboard/configmap.yaml
# Wazuh Dashboard (OpenSearch Dashboards) configuration.

apiVersion: v1
kind: ConfigMap
metadata:
  name: wazuh-dashboard-config
  namespace: wazuh
  labels:
    app: wazuh-dashboard
data:
  opensearch_dashboards.yml: |
    server.host: 0.0.0.0
    server.port: 5601
    server.name: wazuh-dashboard
    server.ssl.enabled: true
    server.ssl.certificate: /usr/share/wazuh-dashboard/certs/dashboard.pem
    server.ssl.key: /usr/share/wazuh-dashboard/certs/dashboard-key.pem

    opensearch.hosts: https://wazuh-indexer-svc:9200
    opensearch.ssl.verificationMode: certificate
    opensearch.ssl.certificateAuthorities:
      - /usr/share/wazuh-dashboard/certs/root-ca.pem
    opensearch.ssl.certificate: /usr/share/wazuh-dashboard/certs/dashboard.pem
    opensearch.ssl.key: /usr/share/wazuh-dashboard/certs/dashboard-key.pem
    opensearch.requestHeadersAllowlist:
      - securitytenant
      - Authorization
    opensearch_security.multitenancy.enabled: false
    opensearch_security.readonly_mode.roles:
      - kibana_read_only

    uiSettings.overrides.defaultRoute: /app/wazuh

  wazuh.yml: |
    hosts:
      - default:
          url: https://wazuh-manager-svc
          port: 55000
          username: wazuh-wui
          password: SecurePassword123!
          run_as: false
